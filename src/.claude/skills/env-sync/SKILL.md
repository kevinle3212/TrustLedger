---
name: env-sync
description: Use when a change adds, removes, renames, or newly requires an environment variable across frontend, API, Docker, Kubernetes, or docs.
---

Canonical version: `src/.agents/skills/env-sync/SKILL.md`. Read and follow that
file; this stub exists only so skill discovery keeps working. Do not add
content here — update the canonical skill instead.
