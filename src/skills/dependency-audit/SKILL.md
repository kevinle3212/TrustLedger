---
name: dependency-audit
description:
    Review npm package freshness, vulnerabilities, devDependencies, and
    overrides.
version: "1.0.0"
---

# Dependency Audit

Use `src/.agents/agents/dependency-auditor.md` as the canonical TrustLedger
brief. Record run output and recommendations in `logs/`.
