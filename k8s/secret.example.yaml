# Copy to secret.yaml, replace the example value, and keep the real file out of
# Git. Prefer External Secrets, Sealed Secrets, or your cloud secret manager in
# shared clusters.
apiVersion: v1
kind: Secret
metadata:
    name: trustledger-frontend-secrets
    namespace: trustledger
    labels:
        app.kubernetes.io/name: trustledger-frontend
        app.kubernetes.io/part-of: trustledger
type: Opaque
stringData:
    ADMIN_API_TOKEN: "replace-me"
    CRON_SECRET: "replace-me"
    HEALTH_CHECK_TOKEN: "replace-me"
    MAGIC_LINK_SECRET: "replace-me"
    NOTIFICATIONS_SECRET: "replace-me"
    RESEND_API_KEY: "replace-me"
    SEPOLIA_RPC_URL: "https://sepolia.example.invalid"
