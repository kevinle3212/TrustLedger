# Type Stubs

The canonical stubs guide lives at `stubs/README.md` in the repository root.

Use it when changing `scripts/models/github_models.py`, Azure AI Inference SDK
imports, or strict Python typing behavior.
