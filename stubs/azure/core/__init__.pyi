"""Namespace package marker for the ``azure.core`` type stubs.

Groups the hand-written stubs for the ``azure.core`` symbols that
``scripts/models/github_models.py`` relies on: the ``AzureKeyCredential`` used
for key-based auth (see ``credentials.pyi``) and the ``HttpResponseError`` it
catches for rate-limit handling (see ``exceptions.pyi``).

This file contains no declarations; it only marks ``azure.core`` as a stub
subpackage so its modules are discoverable.
"""
