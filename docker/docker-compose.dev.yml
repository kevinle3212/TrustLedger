services:
    dev:
        build:
            context: ..
            dockerfile: docker/Dockerfile.dev
        volumes:
            - ..:/app
            - /app/node_modules
        environment:
            - NODE_ENV=development
        env_file:
            - ../.env
        ports:
            - "8545:8545"
        command: npm run node
