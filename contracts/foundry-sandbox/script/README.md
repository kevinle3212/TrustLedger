# Sandbox Scripts

Add focused Foundry scripts here only when a repro needs deployment or fork setup
that does not belong in the primary `contracts/script/` directory.

Scripts must not embed private keys, RPC URLs, or secrets.
