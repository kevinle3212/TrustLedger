# Demo PDF generation dependencies.
# Install with: pip install -r utils/requirements.txt
# Used by utils/generate_contract.py to produce sample-contract.pdf  -  a dummy
# freelance service agreement for TrustLedger demos. The PDF is uploaded to IPFS
# and its keccak256 hash is stored on-chain as contractHash.
reportlab==4.4.10  # PDF layout and rendering engine
types-reportlab==4.5.1.20260521  # Type stubs so mypy/Pylance can resolve reportlab
